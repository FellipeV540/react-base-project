document.addEventListener('DOMContentLoaded', () => {
    // Dados fictícios para o dashboard
    const dadosDashboard = {
        pacientesAtendidos: 1234,
        examesRealizados: 567,
        feedbackPositivo: 95
    };

    // Atualiza os elementos do dashboard com os dados fictícios
    document.getElementById('pacientesAtendidos').innerText = dadosDashboard.pacientesAtendidos;
    document.getElementById('examesRealizados').innerText = dadosDashboard.examesRealizados;
    document.getElementById('feedbackPositivo').innerText = `${dadosDashboard.feedbackPositivo}%`;
});